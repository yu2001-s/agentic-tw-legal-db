# Agent Integration

Supported: Codex plugin skill, generic `AGENTS.md`, Claude Code `CLAUDE.md` plus `.claude/commands/`.

Not shipped: MCP wrapper.

First calls:

```bash
twlaw sources status --json
twlaw agent guide --json
```

Use cached workflows for parallel work and cite official `source_url` fields.
