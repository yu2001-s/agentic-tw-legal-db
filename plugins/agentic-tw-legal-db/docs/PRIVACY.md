# Privacy

Local CLI. No telemetry or hosted service.

Live commands may send query terms to official Taiwan government sites. Cached/bundled commands can run locally. Cache data may live under `~/.cache/twlaw/` or `TWLAW_CACHE_DIR`.

No government API tokens are required by default. Output is research material, not legal advice.
